<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartLibrary22">
        <activity android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RegistrationActivity" />
        <activity android:name=".ui.ForgotPasswordActivity" />
        <activity android:name=".ui.ResetPasswordActivity" />
        <activity android:name=".ui.LandingActivity" />
        <activity android:name=".ui.AddBookActivity" />
        <activity android:name=".ui.UserDetailsActivity" />
        <activity android:name=".ui.AddUserActivity" />
        <activity android:name=".ui.AssignBookActivity" />
        <activity android:name=".ui.ViewAssignmentsActivity" />
        <activity android:name=".ui.EmptyActivity" />
    </application>

</manifest>
